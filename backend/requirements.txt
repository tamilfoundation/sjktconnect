# Django core
# Django>=5.2.15 — closes CVE-2026-25673/25674 + PYSEC-2026-48/49/50/51/52/
# 53/54/55/197/198/199/200/201 (16 CVEs, audit 2026-06-26). Holding on 5.2.x
# (LTS) rather than jumping to 6.0 to keep the upgrade surgical.
Django>=5.2.15,<6.0
gunicorn>=21.0
python-dotenv>=1.0

# Database
psycopg2-binary>=2.9
dj-database-url>=2.1

# Static files
whitenoise>=6.6

# Excel import
openpyxl>=3.1

# PDF text extraction
pdfplumber>=0.10

# HTTP requests + HTML parsing
requests>=2.31
beautifulsoup4>=4.12

# AI analysis
google-genai>=1.0
markdown>=3.6

# Gmail API (feedback inbox)
google-api-python-client>=2.0
google-auth>=2.38.0,<3.0

# GeoJSON / boundary polygons
shapely>=2.0

# REST API
djangorestframework>=3.15
django-cors-headers>=4.3

# News Watch (RSS + article extraction)
feedparser>=6.0
trafilatura>=2.0
lxml_html_clean>=0.4
googlenewsdecoder>=0.1

# QR code generation
qrcode[pil]>=7.0

# Image storage (Supabase Storage via S3-compatible API)
django-storages[boto3]>=1.14
# Pillow>=12.2.0 — closes CVE-2026-25990 / 40192 / 42309 / 42310 / 42311 +
# PYSEC-2026-165 (7 CVEs, audit 2026-06-26). Pillow is on the community
# image-upload path (outreach/services/image_processor.py) — accepts
# untrusted user-uploaded image bytes, exactly where parser CVEs bite.
Pillow>=12.2.0
imagehash>=4.3

# Crypto (transitive via google-auth + pdfminer.six)
# cryptography>=46.0.7 — closes PYSEC-2026-35 / PYSEC-2026-36 + GHSA-537c-gmf6-5ccf
# (5 CVEs, audit 2026-06-26). Pinned here because the transitive resolver
# would otherwise leave us on the older 46.0.5 that satisfies google-auth.
cryptography>=46.0.7

# XML / HTML parsing (transitive via trafilatura + via beautifulsoup4)
# lxml>=6.1.0 — closes PYSEC-2026-87 (audit 2026-06-26).
lxml>=6.1.0

# Testing
pytest>=8.0
pytest-django>=4.7
