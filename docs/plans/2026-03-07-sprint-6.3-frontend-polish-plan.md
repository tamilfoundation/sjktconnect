# Sprint 6.3 — Frontend & Polish Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add brief detail pages for linking from reports, update report template for new section structure, commit model upgrade, deploy, and validate end-to-end.

**Architecture:** New Django view + template for individual sitting briefs at `/parliament-watch/brief/<pk>/`. Meeting report template updated to link to brief detail pages. Report HTML sections map to the new prompt structure from Sprint 6.2.

**Tech Stack:** Django 5.x, Next.js 14, Tailwind CSS, Cloud Run

**Prerequisites:** Sprint 6.2 must be complete (new prompt outputs in place).

---

### Task 1: Brief detail page (backend)

**Files:**
- Create: `backend/parliament/templates/parliament/brief_detail.html`
- Modify: `backend/parliament/views.py` (add BriefDetailView)
- Modify: `backend/parliament/urls.py` (add URL pattern)
- Test: `backend/parliament/tests/test_views.py`

URL: `/parliament-watch/brief/<int:pk>/`
Template: renders brief's `summary_html` with proper styling, links back to meeting report.

---

### Task 2: Link briefs from meeting report template

**Files:**
- Modify: `backend/parliament/templates/parliament/meeting_report.html` (or wherever reports render)

In the report display, each sitting date reference links to its brief detail page.

---

### Task 3: Update report template for new sections

**Files:**
- Modify: report display template

Ensure the new section structure (plain summary, scorecard with legend, RPM-aligned signals, checklist) renders properly. Add CSS for taxonomy legend if needed.

---

### Task 4: Commit model upgrade

**Files:**
- Modify: `backend/parliament/management/commands/generate_meeting_reports.py` (already changed, just commit)

The `gemini-3-pro-preview` + `thinking_level="HIGH"` changes were made in the previous session but not committed.

---

### Task 5: Deploy + validate

1. Deploy backend: `gcloud run deploy sjktconnect-api --source . --region asia-southeast1`
2. Deploy frontend: `gcloud run deploy sjktconnect-web --source . --region asia-southeast1`
3. Run migration on production (portfolio field)
4. Run `seed_aliases --clear` on production (Ladang variants)
5. Verify brief detail pages work on tamilschool.org
6. Verify meeting report links to briefs

---

### Task 6: Sprint close

Update CHANGELOG, resolve FLAGS in learner-patterns.md, commit, push.

---

**Estimated files touched: ~10**
**Estimated new tests: ~6**
