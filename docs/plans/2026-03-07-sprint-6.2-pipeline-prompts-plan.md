# Sprint 6.2 — Pipeline Prompts Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Rewrite all three Gemini prompts (mentions, briefs, reports) to use the context builder, enforce past tense, restructure brief output, and produce audience-ready reports with RPM alignment.

**Architecture:** Each generator calls `context_builder.build_context()` and `format_context_for_prompt()` to inject domain context into its prompt. Prompts are rewritten for quality: mention analysis enforces past tense, brief generator produces executive summary → details → quotes, report generator produces layered sections with taxonomy legend.

**Tech Stack:** Django 5.x, google.genai SDK, pytest

**Prerequisites:** Sprint 6.1 must be complete (context JSON, context_builder, portfolio field).

---

### Task 1: Wire context_builder into mention analysis prompt

**Files:**
- Modify: `backend/parliament/services/gemini_client.py:29-64`
- Test: `backend/parliament/tests/test_gemini_client.py`

Add `format_context_for_prompt()` output to `ANALYSIS_PROMPT`. Add instruction: "Write the summary in past tense (e.g. 'raised', 'asked', 'committed'). The speech has already occurred."

---

### Task 2: Rewrite brief generator for prose output

**Files:**
- Modify: `backend/parliament/services/brief_generator.py:121-163`
- Test: `backend/parliament/tests/test_brief_generator.py`

Replace `_build_markdown()` with a Gemini call that produces:
1. **Executive Summary** (2-3 sentences): What happened in this sitting
2. **Detail** (per-mention): Who said what, in what context, any debate/response
3. **Verbatim Quotes** (if significant): Exact quotes from Hansard

Target: 100-350 words. Wire context_builder for glossary and cabinet reference.

---

### Task 3: Rewrite report generator prompt

**Files:**
- Modify: `backend/parliament/management/commands/generate_meeting_reports.py:35-127`
- Test: existing report generation tests

Restructure `MEETING_REPORT_PROMPT` sections:
1. **Plain-language summary** (30-second read, no acronyms)
2. **What Parliament did** (decisions with context)
3. **MP Scorecard** (with taxonomy legend inline)
4. **Executive Responses** (with attribution + credibility assessment)
5. **Policy Signals** (RPM-aligned: which RPM commitment does this address?)
6. **What to Watch** (actionable checklist, not paragraphs)

Wire `format_context_for_prompt()` into the prompt.

---

### Task 4: Validation — regenerate 3rd Meeting 2025

**Files:** None (runtime validation)

Run: `python manage.py generate_meeting_reports --meeting "3rd Meeting 2025"`

Compare output against FLAG-001 through FLAG-005 from `docs/quality/learner-patterns.md`:
- [ ] No minister hallucination (cabinet reference injected)
- [ ] Executive responses attributed to named ministers
- [ ] Past tense in all mention summaries
- [ ] Acronyms expanded on first use
- [ ] Impact categories less skewed toward "General Rhetoric"

---

### Task 5: Sprint close

Update CHANGELOG, learner-patterns.md (resolve flags), commit, push.

---

**Estimated files touched: ~8**
**Estimated new tests: ~10**
