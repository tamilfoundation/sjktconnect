# Sprint 6.1 — Foundation & Data Layer Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Build the data foundation (context JSON, context builder service, MP portfolio, alias fix, dedup fix, rubric update) that Sprint 6.2's prompt rewrites depend on.

**Architecture:** A versioned JSON file (`data/report-context.json`) holds curated domain expertise. A `context_builder.py` service loads this JSON and enriches it with runtime data (MP portfolios, school names) to produce a merged context dict. This dict is passed to Gemini prompts in later sprints. The MP model gets a `portfolio` field so the evaluator can verify minister attributions. The dedup command and alias seeder get targeted fixes.

**Tech Stack:** Django 5.x, PostgreSQL (Supabase), pytest, google.genai SDK

---

### Task 1: Save Context JSON v2.0

**Files:**
- Create: `backend/data/report-context.json`

**Step 1: Create the context JSON file**

```json
{
  "version": "2.0",
  "last_updated": "2026-03-07",
  "report_config": {
    "output_language": "en",
    "style": "journalistic-neutral",
    "audience": "School boards, PTA leaders, community stakeholders, journalists",
    "tone": "Report, do not analyse. What was said, by whom, in what context."
  },
  "domain": {
    "focus": "SJK(T) Tamil vernacular schools in Malaysia",
    "system_context": "SJK(T) schools operate under Article 152 and the Education Act 1996. They follow the national curriculum (KSSR/KSSM) with Tamil as medium of instruction for core subjects.",
    "challenges": [
      "Low enrolment: 154 of 528 schools have fewer than 150 students",
      "Rural isolation: Many schools in former estates (ladang) with declining populations",
      "Urban space constraints: Schools in cities face land and building limitations",
      "Three-language burden: Students must master Tamil, Bahasa Melayu, and English",
      "Insufficient preschools: Limited pre-school provision feeds into low Year 1 intake",
      "Long travel distances: Parents travel far to send children to Tamil schools, adding cost",
      "Curriculum support gaps: MOE may overlook Tamil-specific curriculum needs",
      "Perception of second-class status: Feeling of being passed over compared to SK schools",
      "Teacher shortages: Difficulty recruiting Tamil-proficient teachers in some areas",
      "Infrastructure decay: Ageing buildings, especially in estate schools"
    ]
  },
  "cabinet": {
    "education": {
      "minister": "Dato' Sri Fadhlina binti Sidek",
      "portfolio": "Minister of Education",
      "since": "2022-12-03"
    },
    "finance": {
      "minister": "YAB Dato' Seri Anwar bin Ibrahim",
      "portfolio": "Minister of Finance (Prime Minister)",
      "since": "2022-11-24"
    },
    "health": {
      "minister": "Datuk Seri Dr. Zaliha binti Mustafa",
      "portfolio": "Minister of Health",
      "since": "2022-12-03",
      "note": "NOT the Minister of Education — do not confuse"
    },
    "unity": {
      "minister": "Dato' Aaron Ago Dagang",
      "portfolio": "Minister of National Unity",
      "since": "2022-12-03"
    }
  },
  "glossary": {
    "SJK(T)": "Sekolah Jenis Kebangsaan (Tamil) — Tamil-medium national-type school",
    "SJK(C)": "Sekolah Jenis Kebangsaan (Cina) — Chinese-medium national-type school",
    "SK": "Sekolah Kebangsaan — national school (Malay-medium)",
    "PPKI": "Program Pendidikan Khas Integrasi — Special Education Integration Programme",
    "JPN": "Jabatan Pendidikan Negeri — State Education Department",
    "PPD": "Pejabat Pendidikan Daerah — District Education Office",
    "KPM": "Kementerian Pendidikan Malaysia — Ministry of Education Malaysia",
    "KSSR": "Kurikulum Standard Sekolah Rendah — Primary School Standard Curriculum",
    "KSSM": "Kurikulum Standard Sekolah Menengah — Secondary School Standard Curriculum",
    "KP 2027": "Kurikulum Persekolahan 2027 — new national curriculum framework",
    "RPM": "Rancangan Pendidikan Malaysia — Malaysia Education Plan",
    "MBPK": "Mesyuarat Bersama Pegawai Kewangan — Joint Financial Officers Meeting",
    "BM": "Bahasa Melayu — Malay language",
    "DUN": "Dewan Undangan Negeri — State Legislative Assembly",
    "DR": "Dewan Rakyat — House of Representatives"
  },
  "taxonomy": {
    "stance": {
      "Advocacy": "MP actively pushing for resources or improvements for Tamil schools",
      "Inquiry": "MP asking questions to understand current status or plans",
      "Critical": "MP criticising government or policy handling of Tamil school issues"
    },
    "impact": {
      "Policy Shift": "Government changes or announces new policy affecting Tamil schools",
      "Budget Allocation": "Specific funding amount committed or requested for Tamil schools",
      "Localised Issue": "Issue specific to one school or constituency (e.g. infrastructure repair)",
      "General Rhetoric": "Broad statements about Tamil schools without specific commitments or actions. Use ONLY when the MP's statement is genuinely non-specific."
    },
    "verdict": {
      "Commitment Made": "Minister made a specific, time-bound promise (e.g. 'will rebuild by Q3 2026')",
      "Resolved": "Issue has been actioned and completed as confirmed by minister",
      "Deflected": "Minister redirected the question or gave a non-answer",
      "Unanswered": "Question received no ministerial response within the meeting period"
    }
  },
  "national_education_plan": {
    "name": "Rancangan Pendidikan Malaysia 2026-2035 (RPM)",
    "status": "Approved by Cabinet, implementation beginning 2026",
    "relevance": "The RPM is the government's own strategic commitment. Framing Tamil school advocacy as holding government accountable to RPM commitments is more effective than special pleading.",
    "key_commitments": [
      "Equitable access to quality education regardless of school type",
      "Infrastructure upgrades for under-resourced schools",
      "Teacher training and placement in underserved areas",
      "Curriculum support for vernacular education streams",
      "Pre-school expansion to improve school readiness"
    ],
    "kp_2027": {
      "name": "Kurikulum Persekolahan 2027",
      "relevance": "New national curriculum — vehicle for implementing RPM commitments including Tamil language provision"
    }
  },
  "national_baseline": {
    "total_sjkt": 528,
    "total_students": 69900,
    "total_teachers": 5460,
    "under_enrolled_schools": 154,
    "under_enrolled_threshold": 150,
    "constituencies_with_sjkt": 222,
    "states": 13
  },
  "session_history": {
    "15th_parliament": {
      "term": "2022-2027",
      "meetings_analysed": 13,
      "total_mentions": 204,
      "mp_scorecards": 53
    }
  }
}
```

**Step 2: Verify the file is valid JSON**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect && python -c "import json; json.load(open('backend/data/report-context.json')); print('Valid JSON')"`
Expected: `Valid JSON`

**Step 3: Commit**

```bash
git add backend/data/report-context.json
git commit -m "feat: add report context JSON v2.0 with cabinet, glossary, RPM, taxonomy"
```

---

### Task 2: Build context_builder Service

**Files:**
- Create: `backend/parliament/services/context_builder.py`
- Create: `backend/parliament/tests/test_context_builder.py`

**Step 1: Write the failing tests**

```python
"""Tests for context_builder service."""
import json
from pathlib import Path
from unittest.mock import patch

from django.test import TestCase

from parliament.services.context_builder import build_context, load_context_json


class LoadContextJsonTest(TestCase):
    def test_loads_valid_json(self):
        """load_context_json returns a dict with version key."""
        ctx = load_context_json()
        self.assertIsInstance(ctx, dict)
        self.assertEqual(ctx["version"], "2.0")

    def test_has_required_sections(self):
        """Context JSON has all required top-level keys."""
        ctx = load_context_json()
        for key in ("cabinet", "glossary", "taxonomy", "national_baseline",
                     "national_education_plan", "domain"):
            self.assertIn(key, ctx, f"Missing key: {key}")


class BuildContextTest(TestCase):
    def test_returns_dict_with_static_and_runtime(self):
        """build_context merges static JSON with runtime data."""
        ctx = build_context()
        self.assertIn("cabinet", ctx)
        self.assertIn("school_names", ctx)
        self.assertIn("mp_portfolios", ctx)

    def test_school_names_is_list(self):
        """Runtime school names should be a list."""
        ctx = build_context()
        self.assertIsInstance(ctx["school_names"], list)

    def test_mp_portfolios_is_list(self):
        """Runtime MP portfolios should be a list."""
        ctx = build_context()
        self.assertIsInstance(ctx["mp_portfolios"], list)
```

**Step 2: Run tests to verify they fail**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_context_builder.py -v`
Expected: FAIL with `ModuleNotFoundError` or `ImportError`

**Step 3: Write minimal implementation**

```python
"""Context builder — assembles domain context for Gemini prompts.

Loads the curated report-context.json and enriches it with runtime
data (MP portfolios, school names) for injection into prompts.
"""

import json
import logging
from pathlib import Path

logger = logging.getLogger(__name__)

CONTEXT_JSON_PATH = Path(__file__).resolve().parent.parent.parent / "data" / "report-context.json"


def load_context_json() -> dict:
    """Load the versioned context JSON file.

    Returns the parsed dict. Raises FileNotFoundError if missing.
    """
    with open(CONTEXT_JSON_PATH) as f:
        return json.load(f)


def build_context() -> dict:
    """Build full context dict: static JSON + runtime data.

    Returns a dict with all static context plus:
    - school_names: list of active school short names
    - mp_portfolios: list of dicts with mp name and portfolio
    """
    from parliament.models import MP
    from schools.models import School

    ctx = load_context_json()

    # Runtime: school names for linkification verification
    ctx["school_names"] = list(
        School.objects.filter(is_active=True)
        .values_list("short_name", flat=True)
    )

    # Runtime: MP portfolios for minister attribution verification
    ctx["mp_portfolios"] = list(
        MP.objects.exclude(portfolio="")
        .values("name", "portfolio", "constituency__name")
    )

    return ctx


def format_context_for_prompt(ctx: dict) -> str:
    """Format context dict as a text block for prompt injection.

    Returns a string suitable for appending to a Gemini prompt.
    """
    sections = []

    # Cabinet reference
    cabinet = ctx.get("cabinet", {})
    if cabinet:
        lines = ["CABINET REFERENCE (use to verify minister names):"]
        for key, info in cabinet.items():
            lines.append(f"- {info['portfolio']}: {info['minister']}")
        sections.append("\n".join(lines))

    # Glossary
    glossary = ctx.get("glossary", {})
    if glossary:
        lines = ["GLOSSARY (expand these acronyms on first use):"]
        for abbr, defn in glossary.items():
            lines.append(f"- {abbr}: {defn}")
        sections.append("\n".join(lines))

    # Taxonomy definitions
    taxonomy = ctx.get("taxonomy", {})
    if taxonomy:
        lines = ["TAXONOMY DEFINITIONS:"]
        for category, values in taxonomy.items():
            lines.append(f"\n{category.title()}:")
            for label, defn in values.items():
                lines.append(f"  - {label}: {defn}")
        sections.append("\n".join(lines))

    # National baseline
    baseline = ctx.get("national_baseline", {})
    if baseline:
        lines = [
            "NATIONAL BASELINE:",
            f"- {baseline.get('total_sjkt', 528)} SJK(T) schools",
            f"- {baseline.get('total_students', 69900)} students",
            f"- {baseline.get('total_teachers', 5460)} teachers",
            f"- {baseline.get('under_enrolled_schools', 154)} under-enrolled (< {baseline.get('under_enrolled_threshold', 150)} students)",
        ]
        sections.append("\n".join(lines))

    # RPM reference
    rpm = ctx.get("national_education_plan", {})
    if rpm:
        lines = [f"NATIONAL EDUCATION PLAN: {rpm.get('name', 'RPM 2026-2035')}"]
        lines.append(f"Status: {rpm.get('status', '')}")
        lines.append(f"Relevance: {rpm.get('relevance', '')}")
        for commitment in rpm.get("key_commitments", []):
            lines.append(f"  - {commitment}")
        sections.append("\n".join(lines))

    # MP portfolios (runtime)
    portfolios = ctx.get("mp_portfolios", [])
    if portfolios:
        lines = ["MP PORTFOLIOS (for attribution verification):"]
        for mp in portfolios:
            lines.append(f"- {mp['name']}: {mp['portfolio']} ({mp.get('constituency__name', '')})")
        sections.append("\n".join(lines))

    return "\n\n".join(sections)
```

**Step 4: Run tests to verify they pass**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_context_builder.py -v`
Expected: All 5 tests PASS

**Step 5: Commit**

```bash
git add parliament/services/context_builder.py parliament/tests/test_context_builder.py
git commit -m "feat: add context_builder service — loads JSON + runtime data for prompts"
```

---

### Task 3: Add `portfolio` Field to MP Model

**Files:**
- Modify: `backend/parliament/models.py:153-178` (MP class)
- Create: migration file (auto-generated)
- Modify: `backend/parliament/tests/test_context_builder.py` (add portfolio test)

**Step 1: Write the failing test**

Add to `backend/parliament/tests/test_context_builder.py`:

```python
from parliament.models import MP
from schools.models import Constituency


class MPPortfolioTest(TestCase):
    def test_mp_portfolio_field_exists(self):
        """MP model has a portfolio CharField."""
        field = MP._meta.get_field("portfolio")
        self.assertEqual(field.__class__.__name__, "CharField")

    def test_portfolio_default_blank(self):
        """Portfolio defaults to empty string."""
        field = MP._meta.get_field("portfolio")
        self.assertEqual(field.default, "")

    def test_build_context_includes_portfolio(self):
        """build_context returns MPs with portfolio data."""
        # Create test constituency and MP
        from schools.models import Constituency
        c = Constituency.objects.create(
            code="P999", name="Test", state="Test",
        )
        MP.objects.create(
            constituency=c,
            name="Test Minister",
            portfolio="Minister of Education",
        )
        ctx = build_context()
        portfolios = ctx["mp_portfolios"]
        self.assertEqual(len(portfolios), 1)
        self.assertEqual(portfolios[0]["portfolio"], "Minister of Education")
```

**Step 2: Run test to verify it fails**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_context_builder.py::MPPortfolioTest -v`
Expected: FAIL with `FieldDoesNotExist`

**Step 3: Add the portfolio field to MP model**

In `backend/parliament/models.py`, after line 170 (`mymp_slug` field), add:

```python
    portfolio = models.CharField(
        max_length=200, blank=True, default="",
        help_text="Ministerial portfolio, e.g. 'Minister of Education'",
    )
```

**Step 4: Generate and apply migration**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python manage.py makemigrations parliament -n add_mp_portfolio && python manage.py migrate`
Expected: Migration created and applied

**Step 5: Run tests to verify they pass**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_context_builder.py -v`
Expected: All 8 tests PASS

**Step 6: Commit**

```bash
git add parliament/models.py parliament/migrations/ parliament/tests/test_context_builder.py
git commit -m "feat: add portfolio field to MP model for minister attribution"
```

---

### Task 4: Update MP Scraper to Extract Portfolio

**Files:**
- Modify: `backend/parliament/services/mp_scraper.py` (add portfolio extraction)
- Modify: `backend/parliament/management/commands/import_mp_profiles.py` (save portfolio)

**Step 1: Read the mp_scraper to understand current structure**

Read `backend/parliament/services/mp_scraper.py` to find where profile data is parsed from parlimen.gov.my HTML. The portfolio/jawatan is usually on the profile page.

**Step 2: Write the failing test**

Create `backend/parliament/tests/test_mp_portfolio_scraper.py`:

```python
"""Test portfolio extraction from MP profile HTML."""
from django.test import TestCase
from parliament.services.mp_scraper import extract_portfolio_from_html


class ExtractPortfolioTest(TestCase):
    def test_extracts_minister_portfolio(self):
        """Extracts portfolio from profile page HTML."""
        html = '<div class="jawatan">Menteri Pendidikan</div>'
        result = extract_portfolio_from_html(html)
        self.assertIn("Pendidikan", result)

    def test_returns_empty_for_backbencher(self):
        """Returns empty string for MPs without ministerial role."""
        html = '<div class="ahli-biasa">Ahli Parlimen</div>'
        result = extract_portfolio_from_html(html)
        self.assertEqual(result, "")
```

**Step 3: Implement portfolio extraction**

Add `extract_portfolio_from_html()` function to `mp_scraper.py`. The exact implementation depends on the HTML structure of parlimen.gov.my profile pages — inspect the page during implementation.

**Step 4: Update `import_mp_profiles` command to save portfolio**

In the command's `handle()` method, after fetching profile data, save `portfolio` to the MP model:

```python
mp.portfolio = profile_data.get("portfolio", "")
```

**Step 5: Run tests**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_mp_portfolio_scraper.py -v`
Expected: PASS

**Step 6: Commit**

```bash
git add parliament/services/mp_scraper.py parliament/management/commands/import_mp_profiles.py parliament/tests/test_mp_portfolio_scraper.py
git commit -m "feat: extract MP portfolio from parlimen.gov.my profile pages"
```

---

### Task 5: Add `executive_response_attribution` to Evaluator Rubric

**Files:**
- Modify: `backend/parliament/services/evaluator.py:118-127` (REPORT_TIER2_CRITERIA)
- Modify: `backend/parliament/tests/test_evaluator.py` (add criterion test)

**Step 1: Write the failing test**

```python
def test_report_tier2_has_executive_response_attribution(self):
    """REPORT_TIER2_CRITERIA includes executive_response_attribution."""
    from parliament.services.evaluator import REPORT_TIER2_CRITERIA
    self.assertIn("executive_response_attribution", REPORT_TIER2_CRITERIA)
```

**Step 2: Run test to verify it fails**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_evaluator.py -k "executive_response_attribution" -v`
Expected: FAIL

**Step 3: Add the criterion**

In `backend/parliament/services/evaluator.py`, add to `REPORT_TIER2_CRITERIA` (after line 124):

```python
- executive_response_attribution: Is every executive response attributed to a named minister with correct portfolio? (1-10)
```

**Step 4: Run tests**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_evaluator.py -v`
Expected: All PASS

**Step 5: Commit**

```bash
git add parliament/services/evaluator.py parliament/tests/test_evaluator.py
git commit -m "feat: add executive_response_attribution to report quality rubric"
```

---

### Task 6: Fix Mention Deduplication (Same-Speaker Same-Page)

**Files:**
- Modify: `backend/parliament/management/commands/dedup_mentions.py:32-38`
- Create: `backend/parliament/tests/test_dedup_mentions.py`

**Step 1: Write the failing test**

```python
"""Tests for dedup_mentions command."""
from datetime import date

from django.test import TestCase
from django.core.management import call_command

from hansard.models import HansardMention, HansardSitting


class DedupSameSpeakerTest(TestCase):
    def setUp(self):
        self.sitting = HansardSitting.objects.create(
            sitting_date=date(2025, 10, 29),
            pdf_url="https://example.com/test.pdf",
            status="COMPLETED",
            mention_count=3,
        )

    def test_dedup_same_speaker_same_page(self):
        """Mentions with same mp_name and page_number should be merged."""
        HansardMention.objects.create(
            sitting=self.sitting, page_number=5, mp_name="YB Ahmad",
            keyword_matched="SJK(T)", verbatim_quote="Short quote",
            ai_summary="Summary 1", significance=2,
        )
        HansardMention.objects.create(
            sitting=self.sitting, page_number=5, mp_name="YB Ahmad",
            keyword_matched="SJK(T)", verbatim_quote="A much longer quote about schools",
            ai_summary="Summary 2", significance=3,
        )
        HansardMention.objects.create(
            sitting=self.sitting, page_number=5, mp_name="YB Bala",
            keyword_matched="SJK(T)", verbatim_quote="Different speaker",
            ai_summary="Summary 3", significance=2,
        )
        call_command("dedup_mentions")
        remaining = HansardMention.objects.filter(sitting=self.sitting).count()
        # YB Ahmad's 2 mentions merged into 1, YB Bala stays = 2 total
        self.assertEqual(remaining, 2)

    def test_dedup_keeps_highest_significance(self):
        """Keeper should have highest significance."""
        HansardMention.objects.create(
            sitting=self.sitting, page_number=5, mp_name="YB Ahmad",
            keyword_matched="SJK(T)", verbatim_quote="Short",
            ai_summary="Low", significance=1,
        )
        HansardMention.objects.create(
            sitting=self.sitting, page_number=5, mp_name="YB Ahmad",
            keyword_matched="SJK(T)", verbatim_quote="Much longer quote here",
            ai_summary="High", significance=4,
        )
        call_command("dedup_mentions")
        keeper = HansardMention.objects.get(sitting=self.sitting)
        self.assertEqual(keeper.significance, 4)
```

**Step 2: Run tests to verify they fail**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_dedup_mentions.py -v`
Expected: FAIL — current dedup groups by (sitting, page) only, not (sitting, page, mp_name)

**Step 3: Fix the dedup grouping**

In `backend/parliament/management/commands/dedup_mentions.py`, change the grouping at line 33-36:

```python
        # Find all (sitting, page, speaker) groups with more than one mention
        dupes = (
            HansardMention.objects
            .values("sitting_id", "page_number", "mp_name")
            .annotate(cnt=Count("id"))
            .filter(cnt__gt=1)
            .order_by("sitting_id", "page_number")
        )
```

And update the filter at line 47-49 to include `mp_name`:

```python
            mentions = list(
                HansardMention.objects
                .filter(
                    sitting_id=group["sitting_id"],
                    page_number=group["page_number"],
                    mp_name=group["mp_name"],
                )
                .select_related("sitting")
                .order_by("id")
            )
```

**Step 4: Run tests**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest parliament/tests/test_dedup_mentions.py -v`
Expected: All PASS

**Step 5: Run full test suite to check for regressions**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest --tb=short -q`
Expected: All existing tests still PASS

**Step 6: Commit**

```bash
git add parliament/management/commands/dedup_mentions.py parliament/tests/test_dedup_mentions.py
git commit -m "fix: dedup mentions by speaker+page instead of just page"
```

---

### Task 7: Add "Without Ladang" Alias Variant to seed_aliases

**Files:**
- Modify: `backend/hansard/management/commands/seed_aliases.py:56-88`
- Modify: `backend/hansard/tests/test_seed_aliases.py` (or create if doesn't exist)

**Step 1: Write the failing test**

```python
"""Test seed_aliases generates 'without Ladang' variants."""
from django.test import TestCase
from hansard.management.commands.seed_aliases import generate_aliases_for_school
from unittest.mock import Mock


class WithoutLadangAliasTest(TestCase):
    def test_generates_without_ladang_variant(self):
        """Schools with 'Ladang' should get a variant without it."""
        school = Mock()
        school.name = "SEKOLAH JENIS KEBANGSAAN (TAMIL) LADANG SERENDAH"
        school.short_name = "SJK(T) Ladang Serendah"
        school.is_active = True

        aliases = generate_aliases_for_school(school)
        alias_texts = [a["alias"].lower() for a in aliases]

        # Should have "SJK(T) Serendah" variant (without Ladang)
        self.assertTrue(
            any("sjk(t) serendah" in t for t in alias_texts),
            f"Expected 'SJK(T) Serendah' alias, got: {alias_texts}"
        )

    def test_no_ladang_no_extra_alias(self):
        """Schools without 'Ladang' should NOT get an extra alias."""
        school = Mock()
        school.name = "SEKOLAH JENIS KEBANGSAAN (TAMIL) TAMAN MELAWATI"
        school.short_name = "SJK(T) Taman Melawati"
        school.is_active = True

        aliases_before = generate_aliases_for_school(school)
        # Count should not include a "without Ladang" variant
        alias_texts = [a["alias"].lower() for a in aliases_before]
        self.assertFalse(
            any("without" in t for t in alias_texts),
        )
```

**Step 2: Run tests to verify they fail**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest hansard/tests/test_seed_aliases.py -k "WithoutLadang" -v 2>/dev/null || python -m pytest parliament/tests/test_seed_aliases.py -k "WithoutLadang" -v`
Expected: FAIL — no "without Ladang" variant generated

**Step 3: Add the variant to generate_aliases_for_school**

In `backend/hansard/management/commands/seed_aliases.py`, after the SJKT variant block (line 76), add:

```python
    # 6. "Without Ladang" variant — MPs commonly drop "Ladang" when referencing
    # estate schools (e.g. "SJK(T) Serendah" instead of "SJK(T) Ladang Serendah")
    if school.short_name and re.search(r"\bLadang\b", school.short_name, re.IGNORECASE):
        without_ladang = re.sub(
            r"\s*\bLadang\b\s*",
            " ",
            school.short_name,
            flags=re.IGNORECASE,
        ).strip()
        # Clean up double spaces
        without_ladang = re.sub(r"\s+", " ", without_ladang)
        if without_ladang and without_ladang.lower() != school.short_name.lower():
            add(without_ladang, SchoolAlias.AliasType.COMMON)
```

**Step 4: Run tests**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest hansard/tests/ -k "WithoutLadang or seed_alias" -v`
Expected: All PASS

**Step 5: Commit**

```bash
git add hansard/management/commands/seed_aliases.py hansard/tests/test_seed_aliases.py
git commit -m "feat: add 'without Ladang' alias variant for 294 estate schools (FLAG-003)"
```

---

### Task 8: Create WAT Workflow for Context Maintenance

**Files:**
- Create: `Settings/_workflows/context-maintenance.md` (at workspace level)

**Step 1: Write the workflow**

```markdown
# Context Maintenance Workflow

## Objective
Keep `data/report-context.json` accurate and current. This is the single source
of domain expertise for all Gemini-powered outputs (mentions, briefs, reports).

## When to Update

| Trigger | What to update | Example |
|---------|---------------|---------|
| Cabinet reshuffle | `cabinet` section | New Minister of Education appointed |
| New Parliament session | `session_history` | 16th Parliament begins |
| MOE policy change | `national_education_plan` | RPM updated or replaced |
| New acronym in Hansard | `glossary` | New programme acronym appears |
| Taxonomy confusion in reports | `taxonomy` | Impact category definition refined |
| Annual statistics update | `national_baseline` | MOE releases new enrolment data |
| Quality flag raised | Any relevant section | FLAG from learner-patterns.md |

## How to Update

1. Open `backend/data/report-context.json`
2. Make the change
3. Bump `version` (minor for additions, major for structural changes)
4. Update `last_updated` to today's date
5. Run: `python -c "import json; json.load(open('backend/data/report-context.json')); print('Valid')"`
6. Commit: `git commit -m "data: update report context vX.Y — [what changed]"`

## Version Scheme

- `2.0` → `2.1`: Added new glossary term, updated baseline stat
- `2.1` → `3.0`: Restructured a section, added new top-level key

## Do NOT

- Add speculative or unverified information
- Let the file exceed 50 KB (currently ~5 KB)
- Add runtime data (school lists, MP rosters) — that's context_builder.py's job
- Duplicate data already in the database (school names, constituency codes)
```

**Step 2: Commit**

```bash
git add Settings/_workflows/context-maintenance.md
git commit -m "docs: WAT workflow for report context JSON maintenance"
```

---

### Task 9: Run Full Test Suite + Sprint Close

**Step 1: Run full backend test suite**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python -m pytest --tb=short -q`
Expected: All tests PASS, count should be ~910+ (898 existing + new tests)

**Step 2: Run seed_aliases in dry-run mode to verify Ladang aliases**

Run: `cd C:/Users/tamil/Python/Development/SJKTConnect/backend && python manage.py seed_aliases --clear 2>&1 | tail -5`
Expected: Higher alias count than before (previous was ~2100, should be ~2400+ with Ladang variants)

**Step 3: Update CHANGELOG.md**

Append Sprint 6.1 entry to CHANGELOG.md.

**Step 4: Final commit + push**

```bash
git add -A
git commit -m "docs: Sprint 6.1 changelog — foundation & data layer"
git push
```

---

## Summary of Deliverables

| # | Deliverable | Files | Tests |
|---|------------|-------|-------|
| 1 | Context JSON v2.0 | `data/report-context.json` | JSON validation |
| 2 | context_builder service | `parliament/services/context_builder.py` | 5 tests |
| 3 | MP portfolio field | `parliament/models.py` + migration | 3 tests |
| 4 | Portfolio scraper | `parliament/services/mp_scraper.py` | 2 tests |
| 5 | Rubric update | `parliament/services/evaluator.py` | 1 test |
| 6 | Dedup fix | `parliament/management/commands/dedup_mentions.py` | 2 tests |
| 7 | Ladang alias | `hansard/management/commands/seed_aliases.py` | 2 tests |
| 8 | WAT workflow | `Settings/_workflows/context-maintenance.md` | — |
| 9 | Sprint close | CHANGELOG, push | Full suite |

**Estimated new tests: ~15**
**Estimated files touched: ~12**
